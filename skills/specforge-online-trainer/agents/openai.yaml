interface:
  display_name: "SpecForge Online Trainer"
  short_description: "One-command online training with regen, planning, and optional Gmail notifications"
  default_prompt: "Run a full SpecForge online training pipeline using a spec file: prepare data, optional regen, automatic parallel planning, and start training (EAGLE3 or DFlash). Before execution, ask whether to enable Gmail notifications."
